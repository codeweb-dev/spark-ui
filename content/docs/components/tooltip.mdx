---
title: Tooltip
description: A short label shown when hovering or focusing an element.
category: Layout
---

The Tooltip shows a short label on hover or focus. Wrap your app (or the demo area) in `TooltipProvider`, then compose `Tooltip`, `TooltipTrigger`, and `TooltipContent`.

## Preview

<ComponentPreview name="tooltip" />

## Usage

### Installation

<InstallBlock command="tooltip" />

## Properties

<PropsTable>
  <Prop
    name="delayDuration"
    type='number'
    default='700'
    description="TooltipProvider only: delay in ms before tooltips open."
  />
  <Prop
    name="sideOffset"
    type='number'
    default='4'
    description="TooltipContent only: distance from the trigger."
  />
  <Prop
    name="className"
    type='string'
    default='—'
    description="Additional CSS classes."
  />
</PropsTable>
