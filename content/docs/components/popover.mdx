---
title: Popover
description: A floating panel anchored to a trigger element.
category: Components
---

The Popover displays rich content anchored to a trigger. Compose it from `Popover`, `PopoverTrigger`, and `PopoverContent`.

## Preview

<ComponentPreview name="popover" />

## Installation

<InstallBlock command="popover" />

## Usage

<UsageCode name="popover" />

## Properties

<PropsTable>
  <Prop
    name="align"
    type='"start" | "center" | "end"'
    default='"center"'
    description="PopoverContent only: alignment against the trigger."
  />
  <Prop
    name="sideOffset"
    type='number'
    default='4'
    description="PopoverContent only: distance from the trigger."
  />
  <Prop
    name="className"
    type='string'
    default='—'
    description="Additional CSS classes."
  />
</PropsTable>
