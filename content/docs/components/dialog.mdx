---
title: Dialog
description: A modal window overlaid on the page with an optional close button.
category: Components
---

The Dialog overlays content on the page. Compose it from `Dialog`, `DialogTrigger`, `DialogContent`, `DialogHeader`, `DialogFooter`, `DialogTitle`, `DialogDescription`, and `DialogClose`.

## Preview

<ComponentPreview name="dialog" />

## Installation

<InstallBlock command="dialog" />

## Usage

<UsageCode name="dialog" />

## Properties

<PropsTable>
  <Prop
    name="showCloseButton"
    type='boolean'
    default='true'
    description="DialogContent only: show the corner close button."
  />
  <Prop
    name="open"
    type='boolean'
    default='—'
    description="Controlled open state of the dialog."
  />
  <Prop
    name="onOpenChange"
    type='(open: boolean) => void'
    default='—'
    description="Called when the open state changes."
  />
  <Prop
    name="className"
    type='string'
    default='—'
    description="Additional CSS classes. Available on every subcomponent."
  />
</PropsTable>
