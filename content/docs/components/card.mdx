---
title: Card
description: A container with header, title, description, content, and footer sections.
category: Layout
---

The Card groups related content into a bordered container. Compose it from `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, and `CardFooter` — each accepts standard div props.

## Preview

<ComponentPreview name="card" />

## Installation

<InstallBlock command="card" />

## Usage

<UsageCode name="card" />

## Properties

<PropsTable>
  <Prop
    name="className"
    type="string"
    default="—"
    description="Additional CSS classes. Available on every Card subcomponent."
  />
</PropsTable>
