---
title: Hover Card
description: A card preview shown when hovering a trigger element.
category: Layout
---

The HoverCard previews content behind a link or trigger on hover. Compose it from `HoverCard`, `HoverCardTrigger`, and `HoverCardContent`.

## Preview

<ComponentPreview name="hover-card" />

## Installation

<InstallBlock command="hover-card" />

## Usage

<UsageCode name="hover-card" />

## Properties

<PropsTable>
  <Prop
    name="openDelay"
    type='number'
    default='700'
    description="Delay in ms before the card opens."
  />
  <Prop
    name="closeDelay"
    type='number'
    default='300'
    description="Delay in ms before the card closes."
  />
  <Prop
    name="className"
    type='string'
    default='—'
    description="Additional CSS classes."
  />
</PropsTable>
