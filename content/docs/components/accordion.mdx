---
title: Accordion
description: A vertically stacked set of interactive headings that reveal content.
category: Layout
---

The Accordion component follows the standard shadcn/ui structure and is built on Radix UI primitives.

## Preview

<ComponentPreview name="accordion" />

## Usage

### Installation

<InstallBlock command="accordion" />

### Basic Example

```tsx
import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from "@/components/accordion";

export default function App() {
  return (
    <Accordion type="single" collapsible>
      <AccordionItem value="1">
        <AccordionTrigger>Title</AccordionTrigger>
        <AccordionContent>Content goes here.</AccordionContent>
      </AccordionItem>
    </Accordion>
  );
}
```

## Properties

<PropsTable>
  <Prop
    name="type"
    type='"single" | "multiple"'
    default="—"
    description="Determines if one or multiple items can be open."
  />
  <Prop
    name="collapsible"
    type="boolean"
    default="false"
    description="Whether an open item can be closed by clicking its trigger."
  />
  <Prop
    name="className"
    type="string"
    default="—"
    description="Additional CSS classes for the root container."
  />
</PropsTable>
