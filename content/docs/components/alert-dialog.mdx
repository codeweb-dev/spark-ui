---
title: Alert Dialog
description: A modal dialog that interrupts the user for a confirmation.
category: Layout
---

The AlertDialog interrupts the user with important content and expects a response. Compose it from `AlertDialog`, `AlertDialogTrigger`, `AlertDialogContent`, `AlertDialogHeader`, `AlertDialogFooter`, `AlertDialogTitle`, `AlertDialogDescription`, `AlertDialogAction`, and `AlertDialogCancel`.

## Preview

<ComponentPreview name="alert-dialog" />

## Usage

### Installation

<InstallBlock command="alert-dialog" />

## Properties

<PropsTable>
  <Prop
    name="open"
    type='boolean'
    default='—'
    description="Controlled open state of the dialog."
  />
  <Prop
    name="onOpenChange"
    type='(open: boolean) => void'
    default='—'
    description="Called when the open state changes."
  />
  <Prop
    name="className"
    type='string'
    default='—'
    description="Additional CSS classes. Available on every subcomponent."
  />
</PropsTable>
