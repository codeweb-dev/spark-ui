---
title: Avatar
description: An image with a fallback for representing a user.
category: Components
---

The Avatar displays a user image and falls back to initials when the image is unavailable.

## Preview

<ComponentPreview name="avatar" />

## Installation

<InstallBlock command="avatar" />

## Usage

<UsageCode name="avatar" />

## Properties

<PropsTable>
  <Prop
    name="size"
    type='"sm" | "default" | "lg"'
    default='"default"'
    description="Size of the avatar."
  />
  <Prop
    name="className"
    type="string"
    default="—"
    description="Additional CSS classes."
  />
</PropsTable>
