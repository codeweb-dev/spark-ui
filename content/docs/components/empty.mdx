---
title: Empty
description: An empty state with media, title, description, and content slots.
category: Layout
---

The Empty component communicates "nothing here yet" states. Compose it from `Empty`, `EmptyHeader`, `EmptyMedia`, `EmptyTitle`, `EmptyDescription`, and `EmptyContent`.

## Preview

<ComponentPreview name="empty" />

## Installation

<InstallBlock command="empty" />

## Usage

<UsageCode name="empty" />

## Properties

<PropsTable>
  <Prop
    name="variant"
    type='"default" | "icon"'
    default='"default"'
    description="EmptyMedia only: icon renders a muted rounded tile sized for an icon."
  />
  <Prop
    name="className"
    type="string"
    default="—"
    description="Additional CSS classes. Available on every Empty subcomponent."
  />
</PropsTable>
