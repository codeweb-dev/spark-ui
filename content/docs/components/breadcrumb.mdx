---
title: Breadcrumb
description: A navigation trail showing the current page's location in the hierarchy.
category: Navigation
---

The Breadcrumb displays the path to the current page. Compose it from `Breadcrumb`, `BreadcrumbList`, `BreadcrumbItem`, `BreadcrumbLink`, `BreadcrumbPage`, `BreadcrumbSeparator`, and `BreadcrumbEllipsis`.

## Preview

<ComponentPreview name="breadcrumb" />

## Usage

### Installation

<InstallBlock command="breadcrumb" />

## Properties

<PropsTable>
  <Prop
    name="asChild"
    type='boolean'
    default='false'
    description="BreadcrumbLink only: render the child element instead of an anchor."
  />
  <Prop
    name="className"
    type='string'
    default='—'
    description="Additional CSS classes. Available on every subcomponent."
  />
</PropsTable>
