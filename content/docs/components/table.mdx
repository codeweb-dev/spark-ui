---
title: Table
description: A responsive table with header, body, footer, and caption sections.
category: Layout
---

The Table renders tabular data. Compose it from `Table`, `TableHeader`, `TableBody`, `TableFooter`, `TableRow`, `TableHead`, `TableCell`, and `TableCaption`.

## Preview

<ComponentPreview name="table" />

## Installation

<InstallBlock command="table" />

## Usage

<UsageCode name="table" />

## Properties

<PropsTable>
  <Prop
    name="className"
    type='string'
    default='—'
    description="Additional CSS classes. Available on every subcomponent."
  />
</PropsTable>
